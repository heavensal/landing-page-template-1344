# Copy to .env and fill in values

# --- API (optional) ---
# Set to "true" to fetch FAQs from ContentForge; "false" uses i18n only
CONTENTFORGE_ENABLED=true
# Base API URL (without endpoint suffix)
CONTENTFORGE_URL=https://contentforge.1344.fr/api/v1
# API token for fetching dynamic content (e.g., FAQs)
CONTENTFORGE_API_TOKEN=

# --- i18n toggle ---
# Set to "true" to enable French/English language switcher
# Set to "false" for single-language (French) site
ENABLE_MULTILANG=false

# --- Contact form (forked live sites) ---
# This template’s dialog is a demo and does not POST.
# On a real site, set these to enable ContentForge POST /send_form from the browser.
# PUBLIC_CONTACT_FORM_URL=https://contentforge.1344.fr/api/v1/send_form
# PUBLIC_CONTACT_FORM_TOKEN=

# --- SMTP (for an external mailer / backend only — not read by this static Astro site) ---
# Example: OVH / Zimbra (ssl0.ovh.net). Copy these to the environment of your API or worker.
# SMTP_HOST=ssl0.ovh.net
# SMTP_PORT=465
# SMTP_SECURE=true
# SMTP_USER=contact@example.com
# SMTP_PASS=
# MAIL_FROM=contact@example.com
# MAIL_TO=contact@example.com
